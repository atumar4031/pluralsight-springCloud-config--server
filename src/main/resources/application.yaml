server:
  port: 5001

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/atumar4031/pluralsight-spring-cloud-config
          default-label: master
          search-paths:
            - station*